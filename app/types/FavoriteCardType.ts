// types/FavoriteCardType.ts
export interface FavoriteCardType {
  title: string;
  icon: string;
  pending: number;
  completed: number;
  pendingUrl: string;
  completedUrl: string;
  visible: boolean;
}
